---
title: "Running Routes & Times"
output: 
  html_document:
    css: style.css
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE, message = FALSE, warning = FALSE)
library(leaflet)
library(DT)
library(dplyr)
library(readr)
library(sf)

```

## Anton's Run - Strava Route


<iframe height='405' width='590' frameborder='0' allowtransparency='true' scrolling='yes' src='https://www.strava.com/routes/2976063228100262196/embed'></iframe>


## Runner Times & Leaderboard


```{r }
# Read runner data from your CSV file
runner_times <- read_csv("data/runner_data.csv")

# Create interactive table
DT::datatable(
  runner_times %>%
    arrange(route, time_minutes) %>%
    mutate(
      time_formatted = paste0(floor(time_minutes), ":", 
                             sprintf("%02d", round((time_minutes %% 1) * 60)))
    ) %>%
    select(runner_name, route, time_formatted, date),
  colnames = c("Runner Name", "Route", "Time (MM:SS)", "Date"),
  options = list(
    pageLength = 10,
    searching = TRUE,
    ordering = TRUE
  ),
  class = 'cell-border stripe'
)

```